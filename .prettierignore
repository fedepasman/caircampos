# Skills de terceros vendorizados. No se reformatean: skills-lock.json guarda
# un hash de cada SKILL.md, y cualquier cambio de formato lo invalida y hace
# imposible verificar que el contenido sea el de upstream.
.agents/

# Nota: packages/supabase/src/database.types.ts NO se excluye. Es generado,
# pero scripts/gen-types.mjs lo formatea como parte de la generación: si el
# archivo escrito no coincidiera con el commiteado, `db:types:check` fallaría
# en cada corrida y la verificación dejaría de significar algo.

# Fixtures con violaciones deliberadas: se mantienen tal cual para que las
# reglas de Semgrep sigan matcheando sobre la forma exacta esperada.
.semgrep/fixtures/

# Lo regenera Next.js en cada build; formatearlo solo produce diffs espurios.
next-env.d.ts

# Artefactos de build y dependencias.
node_modules/
.next/
.expo/
.turbo/
dist/
build/
coverage/
pnpm-lock.yaml

# Documentación comercial.
Presupuestos/
